mixin createPostForm()
    .postFormContainer 
        .userImageContainer 
            img(src = "", alt = "User's profile picture")
        .textAreaContainer 
            textarea#tweetTextarea(placeholder = "What's happening?")
            .buttonsContainer 
                button#tweetButton(disabled = "") Post